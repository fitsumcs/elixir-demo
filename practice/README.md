# Practice (Mix)

This folder is the **Mix / OTP** application for the parent repo. The numbered lesson scripts (`01/`, `02/`, …) live in the repository root next to this directory.

```bash
cd practice
mix deps.get
mix compile
mix test
```

See the root `README.md` for how to run each `.exs` lesson with `elixir`.
